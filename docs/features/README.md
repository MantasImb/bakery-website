# Feature PRDs

Use this directory for active or upcoming feature-specific PRDs that are too detailed for the main product PRD.

When a feature is implemented, either delete its PRD or move any still-relevant product behavior into `docs/prd.md` before deleting it.

Temporary implementation plans should not live here. Create them only when needed and delete them after implementation, preserving durable decisions in the main PRD when appropriate.
